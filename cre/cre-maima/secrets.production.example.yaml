# Production secrets template for cre-maima
# Run: cre secrets create ./secrets.production.yaml --target production-settings
# to push this to the Chainlink DON (encrypted on-chain).
secrets:
  LIFI_API_KEY:
    value: "YOUR_PROD_LIFI_API_KEY_HERE"
