# Required for wallet connect (RainbowKit)
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID=

# Optional: LI.FI API key for better rate limits (app/api/maima/routing)
LIFI_API_KEY=

# CRE: server-side and client-side simulation mode (on | off)
CRE_SIMULATION_MODE=off
NEXT_PUBLIC_CRE_SIMULATION_MODE=off
